sudo: false
env:
  matrix:
    - SYNTHESIS=true
    - SIMULATION=true
addons:
  apt:
    packages:
    - python-imaging
    - iverilog
install:
  - pip install coverage
  - pip install coveralls
script:
  - ./.travis.sh
